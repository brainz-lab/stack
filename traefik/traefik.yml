# Traefik static configuration
api:
  insecure: true
  dashboard: true

entryPoints:
  web:
    address: ":80"

providers:
  file:
    filename: /etc/traefik/routes.yml
    watch: true

log:
  level: INFO
